spring:
  datasource:
    url: jdbc:h2:mem:devpulse_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

devpulse:
  jwt:
    secret: test-only-secret-key-for-junit-tests-devpulse-32bytes-minimum
    expiration-ms: 3600000
